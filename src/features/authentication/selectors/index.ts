import { getError } from './getError'
import { getIsError } from './getIsError'
import { getIsLoading } from './getIsLoading'

const selectors = {
  getError,
  getIsError,
  getIsLoading,
}

export { selectors }
